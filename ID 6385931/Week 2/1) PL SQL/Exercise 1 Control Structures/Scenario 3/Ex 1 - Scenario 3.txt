
CREATE TABLE Customer (
  customer_id INT,
  days INT
);

INSERT INTO Customer VALUES (1, 25);
INSERT INTO Customer VALUES (2, 45);
INSERT INTO Customer VALUES (3, 10);
SET SERVEROUTPUT ON;
DECLARE
 Cursor c_Customers is
 select customer_id,days from Customer;
BEGIN
  for i in c_Customers LOOP
  if i.days<30 then
    dbms_output.put_line(' Remainder : Due is less than ' || i.days || ' days');
  
  end if;
  end loop; 

END;
/